import PropTypes from 'prop-types'

const Cooking = ({bookmark}) => {
    return (
        <div>
            
        </div>
    );
};
Cooking.PropTypes={
    bookmark: PropTypes.object
}

export default Cooking;